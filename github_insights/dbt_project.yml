name: 'cardano_insights'
version: '1.0.0'

profile: 'cardano_insights'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  cardano_insights:
    bronze:
      +materialized: table
      +docs:
        node_color: "#8B4513"
    silver:
      +materialized: table
      +docs:
        node_color: "#C0C0C0"
    gold:
      +materialized: table
      +docs:
        node_color: "#FFD700"